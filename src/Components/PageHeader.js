const PageHeader = () => {
  return (
    <header>
    <img alt="Logo" src={null}></img>
      <nav>
          <a href='/contact'>Contact</a>
          <a href='/terms'>Sign In</a>
          <a href='/cart'>Cart</a>
      </nav>
    </header>
  );
};

export default PageHeader;
